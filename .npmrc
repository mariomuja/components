# GitHub Packages registry for @mariomuja scope
@mariomuja:registry=https://npm.pkg.github.com
# Always authenticate (uses NPM_TOKEN env var or ~/.npmrc)
//npm.pkg.github.com/:_authToken=${NPM_TOKEN}

